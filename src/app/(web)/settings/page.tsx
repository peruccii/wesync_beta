import { Metadata } from 'next';
 
export const metadata: Metadata = {
  title: 'Settings | WeSync',
};
const Settings = () => {
 return ( 
  <h1 className="">Settings page</h1>
  );
}
 
export default Settings;