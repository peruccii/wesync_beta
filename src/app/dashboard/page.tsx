'use client'
import { useSession } from "next-auth/react";
import MutateProviders from "../(auth)/login/mutate/mutate_providers";

const HomePage = async () => {

 return ( 
  <h1>PAGINA HOME</h1>
  );
}
 
export default HomePage;